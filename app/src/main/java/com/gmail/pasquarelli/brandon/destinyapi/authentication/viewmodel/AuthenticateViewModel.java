package com.gmail.pasquarelli.brandon.destinyapi.authentication.viewmodel;

import android.arch.lifecycle.ViewModel;

public class AuthenticateViewModel extends ViewModel {
    // Add a call to encrypt token before saving in SharedPreferences
}
